import React from "react";
import Breadcrumbs from "../components/Breadcrumbs";

const BreadcrumbsController = ({ items }) => {
  return <Breadcrumbs items={items} />;
};

export default BreadcrumbsController;
